# Phase 1 runtime
sqlalchemy>=2.0.30
pydantic>=2.7
psycopg[binary]>=3.1.18

# Phase 2 runtime
pyyaml>=6.0
anthropic>=0.28        # optional: only needed for live extraction
google-genai>=0.2.0    # optional: needed for live extraction via Gemini

# Phase 3 runtime
jinja2>=3.1
bandit>=1.7

# Phases 4-6 runtime
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27

# Phase 1 development
pytest>=8.2
pytest-cov>=5.0
ruff>=0.4
black>=24.4
python-pptx>=1.0     # optional: only needed to rebuild the deck (scripts/build_deck.py)

# Future scope
# alembic>=1.13          # if you add persistence

# Target drivers. Not needed to GENERATE or VALIDATE a connector — static
# analysis reads the source, it never imports it. Only the Phase 4 sandbox that
# actually connects needs these installed.
# pymysql>=1.1
# pyodbc>=5.1
